import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DetialScreen = (k) => {
  console.log(k);
  return (
    <View>
      <Text>DetialScreen</Text>
    </View>
  );
};

export default DetialScreen;

const styles = StyleSheet.create({});
